# 变更记录

本文件只记录已经进入当前运行时、玩家能够实际感知的变化。目标 Windows 验收前仍保留在 `Unreleased`。

## Unreleased

- 修复校园终段背景被缓存层和显示层连续采样两次的问题；高画质档恢复为源图每 2 像素采样一次，默认档恢复为每 6 像素一次，并新增纹理实际尺寸断言。修复后独立包默认与高画质五段定速刷新仍均为 49.6 FPS。
- 静态 PNG 改为复用按既定画质采样后的图像，并按文件修改时间和大小自动失效；同进程第二次测试启动由 1.026 秒降到 0.793 秒，约快 22.7%，没有降低默认或高画质档分辨率。
- v23 独立解压包完整回归通过：五场景强制绘制最低 79.0 FPS，默认档定速均为 49.6 FPS，高画质档另一次复测最低 48.2 FPS；15 张高画质运行图重新生成并抽查。
- 新增 `runWindowsPlaytest` 与 `summarizeWindowsValidation`：记录每局缩放、计划／实际路线、玩家二键位、两人确认、五段 FPS 和人工画面／操作判断；只有三档缩放、三局、两路线、两键位及人工确认全部齐全才报告 Windows PASS。
- 试验 1 ms 帧间 park 后五段最低降到 47.2 FPS，且短时 CPU 采样没有改善，已撤销并保留 0.25 ms 配置。
- 帧间空闲由 `Thread.yield` 忙等改为 0.25 ms JVM park，不触发 MATLAB 隐式重绘；配对的 8 次进程 CPU 采样均值约下降 25%，50 Hz 定速刷新仍保持 49.6 FPS。
- 绘制目标由 45 Hz 提高到 50 Hz，扩大高于 40 FPS 的运行余量；默认档完整回归定速最低 49.9 FPS，高画质档定速最低 49.8 FPS，真实主循环北湖为 49.7 FPS。
- 新增 Windows 一键验收入口：三轮五场景 FPS、六组真实四键／五键冲突、显示缩放与屏幕信息统一写入日志。
- 新增 `ValidationMode`：游戏标题实时显示绘制 FPS，退出时按五段保存真人交互帧率；开场检测分开显示玩家二主键位与备用键位。
- 性能检查支持显式测量 1280×720 高画质档；开发 Mac 连续三轮五场景最低 62.6 / 62.4 / 84.0 FPS，并重新生成、打开核对高画质截图。
- 修正主循环显示刷新：计划帧使用完整 `drawnow`，帧间不再用 `pause` 反复触发完整刷新；加入五场景定速完整刷新门，避免把最大渲染吞吐量误报为真正屏幕帧率。

### 2026-09-10 性能与输入稳定性

- 绘制目标最终提高到 50 Hz，物理和输入保持 60 Hz；短按移动、跳跃和道具会保留到下一次物理步；
- 固定坐标轴并用单一世界变换移动前景，镜头外背景和机制停更；鸭鹅、学生、汽车、行人、自行车、校车、云层和网格使用复合图元；
- 重复冰红茶纹理只处理一次，校园终段背景和校车不在开场隐藏创建；不可见的坐标轴工具栏不再实例化；
- 最终代码统一验收五段最低 65.4 FPS，随后两次完整复测最低 62.1 和 82.1 FPS；默认仍为 960×540、普通背景步长 6，没有继续降低画质；
- 目标 Windows 的真实双人操作、键盘多键冲突、三档缩放和连续三局仍未验收。

### 2026-09-09 空间与可玩性修复

- 接入同一世界内的曲线变梨序章；
- 连接地面与入口台阶，补足桥面、桥墩、拱门支撑，明确北湖缺口；
- 修复反向或高跳造成的剧情飞行失控、台阶内部重生、超时复位碰撞延迟、校车压入地面和重复携带；
- 路口车辆沿地面运动，安全等待区不受危险区碰撞连带误伤；
- 校车连续往返，车身、轮胎、树木和鸭鹅轮廓更新；两只梨都进入体育馆区域才通关；
- 新增统一验收入口和从出生点到终点的完整输入旅程；目标 Windows 真人验收仍未完成。


### Added

- 三颗共享像素爱心、两格真实冰红茶图标和六格像素绳力条；
- 两只高密度圆润梨形角色：香梨式黄绿色与暖白鸭梨色，带果柄、极轻明暗、微小浅坑和状态表情；
- 四只实体鹅与四只实体鸭，可阻挡、承载、携带玩家并在持续推挤后退让；
- 羊驼背后后踢预告、孔雀周期弹射和北湖高桥回收路线；
- 用户提供的完整 Chrome 深色超时页，保留 `ERR_CONNECTION_TIMED_OUT`；
- 由用户成功页制作的脱敏运行时纹理，只重新显示授权学号 `1120250036` 与离线占位；
- 横向与纵向死区跟随镜头，支持右三分之一推进和自行车飞行抬升；
- 北理桥四次攀爬上路、行人红绿灯下路及路线锁定；
- 北食堂／徐特立图书馆汇合、自行车／电动车不可避免的右上撞飞和文博中心安全落点；
- 两辆原创绿白校车移动平台、三根双臂路灯和双梨同车进入体育馆的通关条件；
- 文博上车处的校园方位牌，表达文博中心在北、国防文化主题广场／体育馆在南；
- 15 张当前连续世界截图、4 张页面四态截图、2 张 HUD 截图和相关直接测试。

### Changed

- 旧破防状态改为共享爱心；普通动物主动冲撞扣一心，致命车辆／深渊清空，剧情弹射和撞灯不扣心；
- 冰红茶改成纯移动强化，不回血、不减伤；
- 校园网页面改为固定世界平面，页面切换不锁镜头、不淡入淡出、不移动世界矩形；
- 每局第一次接触登录区固定整页切成 Chrome 超时，并撤掉网页承托让双梨无伤坠落复位；
- 第二次改为两只梨共同站上登录区，原地加载约 0.8 秒后把同一平面切成操场成功页；
- 成功页加入连续地面，角色可保持世界坐标直接向右离开；
- 桥名统一为“北理桥”，体育馆成为唯一终点；
- 文博—体育馆段依据校园图保留南北相对关系，但横向距离和校车线路明确作为玩法压缩；
- MATLAB 运行窗口关闭全局图形抗锯齿，以像素风边缘换取低功耗机器上的稳定帧率；高密度梨形曲线仍保留；
- 保留用户工作树中的重力参数 `-25`。

### Removed

- 当前运行链路中的旧破防、随机掉线、按钮局部消失、校园网页面锁镜头、喷泉、乐学和 Lucy 河；
- 对应的旧破防、喷泉、乐学和 Lucy 河测试；
- 运行时成功页源图中的 IP、已用流量、已用时长和账户余额；
- 体育馆之后误加的良乡校门和“中关村校区”尾段。

### Verified

- MATLAB R2025b Code Analyzer 0 个问题；
- 统一烟雾测试全部通过；
- 直接测试通过：爱心与复位、冰红茶隔离、鸭鹅承载／退让／冲撞、羊驼后踢、孔雀弹射、双轴镜头、校园网确定性四态、北理桥路线锁定、自行车必触发、校车携带、撞灯无伤和同车通关；
- v19 默认档工作树／独立包强制渲染最低 71.1 / 69.2 FPS，五段定速完整刷新均为 45.0 FPS；真实主循环北湖 10.0 秒为 44.8 FPS；
- v20 默认档工作树强制渲染最低 69.1 FPS，五段 50 Hz 定速完整刷新最低 49.9 FPS；高画质档最低 49.8 FPS，真实主循环北湖 8.9 秒为 49.7 FPS；
- v21 默认档工作树强制渲染最低 77.5 FPS，默认档与高画质档五段定速刷新均为 49.6 FPS；真实主循环北湖 9.5 秒为 49.2 FPS；
- 当前连续世界、页面、HUD、梨、北理桥、飞行、校车和体育馆截图已实际打开检查。

### Known Issues

- 连续序章缺口已在 2026-09-09 关闭；目标机与真人手感验收仍未完成；
- 尚未在目标 Windows 11 验证真人双人整局、多键冲突、100%／125%／150% 显示缩放和连续三局；
- 北理桥与横向校园路线是有现实外观／拓扑锚点的玩法化表达，不是精确校园测绘；
- 当前工作树尚未提交、推送、建立 Pull Request、合并 `main` 或创建标签。

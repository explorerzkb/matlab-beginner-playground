# 项目协作约定

## 语言与判断

- 默认使用清晰、连贯、准确且可执行的中文。
- 重要结论区分已确认事实、规则、经验判断、合理推断和待验证事项。
- 工程、AI、网络与软件术语首次关键出现时，解释它是什么以及在当前任务中的作用。
- 不用套话、机械分点或无关背景制造严谨感；保留会影响实现和判断的条件、因果与边界。

## 当前唯一有效文档

开发前按以下顺序读取：

1. `docs/current-project-state.md`；
2. `docs/game-spec-v1.md`；
3. `docs/implementation-plan.md`；
4. 当前任务明确需要时，再读 `assets/reference/README.md` 及其指向的具体素材。

如果这些文件互相冲突，以用户最新明确决定为最高优先级，其次依次为 `current-project-state.md`、`game-spec-v1.md`、`implementation-plan.md`。发现冲突时必须同步修正文档，不能自行从旧材料拼接需求。

## 历史区隔离规则

- `archive/` 中的内容已经失去需求和设计决策权，只是不可变的历史快照。
- 默认不得读取、全文搜索、总结或引用 `archive/`；仓库检索应显式排除该目录。
- 只有用户明确要求追溯历史决定，或当前有效文档点名要求核对某项历史证据时，才能打开被点名的具体文件。不得为了“补充背景”浏览整个历史区。
- 历史文档不得覆盖当前规格。若其中出现更详细的旧参数、旧关卡或旧画风，一律视为已撤销信息。
- 不维护历史文件中的链接、数字或状态；变化原因只写入当前状态文件的简短决策记录。

## 显性项目记忆

- 需求、决策、验证结果和待办必须保存在仓库内的当前有效文件中，不能只依赖聊天历史或隐式记忆。
- 已冻结的玩法与范围写入 `docs/game-spec-v1.md`；实现阶段、验证证据和下一步写入 `docs/current-project-state.md`。
- 发生需求变化时，先更新规格中的对应条目，并在状态文件记录日期、变化和原因。
- 尚未确定的想法只能放入状态文件的“待验证”部分，不得混入已冻结规格。

## 当前开发原则

- 这是面向 MATLAB 初学者的课程大作业，最终程序必须在目标 Windows 电脑上可靠运行。
- 趣味性和双人可操作性优先；物理规律应成为运动、碰撞和绳力的一部分，而不是独立参数演示。
- 视觉、音效、输入映射和关卡数据集中管理，不散落在主循环与物理代码中。
- 第一版使用函数和结构体，不建立复杂继承体系；代码需要让学生能从 `startGame.m` 追到输入、物理、关卡和渲染。
- `assets/reference/` 是设计研究输入，不是 MATLAB 运行时资产；正式资源统一放入 `assets/game/`。
- 核心游戏不联网、不读取真实校园账号、不依赖外部服务，也不把 Symbolic Math Toolbox 变成必需依赖。
- 开发按 `docs/implementation-plan.md` 的阶段门推进。没有通过当前阶段的最低验证时，不用美术堆叠掩盖核心问题。

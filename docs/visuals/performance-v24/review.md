# v24 画质回退修复与性能复核

继续检查 v23 图片缓存接线时发现，校园终段背景被采样了两次：`readGameImage` 已按 `backgroundTextureStride` 采样，创建 `campusBackdrop` 时又对结果执行相同步长。1672×941 源图在高画质档因此只有约 418×236 的实际纹理，而不是一次采样后的 836×471；默认档的有效步长则从 6 变成 36。

现已删除显示层的第二次采样。`testCampusPresentation` 新增实际 `CData` 尺寸断言，要求行列数严格等于源图按当前档位采样一次的结果。它和通用缓存测试分别负责“没有重复采样”与“缓存命中／素材变化后失效”。

修复后重新生成 15 张 1280×720 高画质运行图，并实际打开 `campus-bus-lamps.png`。建筑立面、草地、道路纹理、校车、两只梨、HUD 和路灯清楚可辨，先前截图中不必要的背景像素化已经消失。

工作树统一验收通过：Code Analyzer 零问题，烟雾测试、连续序章和四条完整输入旅程全部通过；五段强制绘制为 80.4 / 100.9 / 77.4 / 97.1 / 150.4 FPS，最低 77.4 FPS；默认档和高画质档五段定速完整刷新均为 49.6 FPS。

最终 ZIP 在全新临时目录解压后再次通过统一验收：强制绘制为 80.3 / 100.8 / 74.5 / 85.0 / 154.6 FPS，最低 74.5 FPS；默认档和高画质档五段定速完整刷新均为 49.6 FPS。包内 Windows 汇总仍为 0/3、INCOMPLETE，不能用开发 Mac 结果替代目标机真人验收。

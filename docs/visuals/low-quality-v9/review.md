# 默认低画质验证（2026-09-09）

用户要求通过降低画质改善帧率。默认窗口由 1280×720 改为 960×540，背景纹理采样步长由 2 改为 6。网页纹理采样保持 2，角色和车辆仍使用原有绘制。物理步长、重力 -25、车辆伤害和复活逻辑未改。

本机 MATLAB R2025b 独立运行 runPerformanceCheck：120 帧强制绘制，20.2 FPS，达到原有 20 FPS 断言，余量较小。此前 1280×720 约 14–18 FPS；分辨率和纹理精度都发生变化。30 Hz 仍是绘制目标，不是实测保证。Windows 未测。

全部冒烟测试通过，包含七个检查点各两次死亡后立即绘制的 14 例复活回归；日志为 regression.txt。Code Analyzer 零问题。

road.png 是默认 960×540 窗口的 getframe 截图，开发机 Retina 输出为 1920×1080。已实际打开检查：背景块状感明显增加，角色轮廓、校车、道路边缘和 HUD 文字仍可辨认。截图为摆放状态的显示检查，不作为通关或车辆伤害证据。

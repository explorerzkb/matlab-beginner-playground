# 校园网登录页运行时使用说明

复核日期：2026-09-03
源文件：`assets/reference/bit-campus-network-login-2026.png`
状态：已完成进入运行时目录前检查和运行时副本复核；正式发布包仍需再检查

## 已确认的画面内容

已实际打开源图。页面包含北京理工大学标识与“校园网用户认证”标题、操场背景、半透明登录框、用户名／密码输入区域、蓝绿色按钮，以及红色保密提示。当前截图中没有看到已填写的账号或密码。

这次检查只证明当前文件的可见像素，不代表以后加工后的副本或发布包自动安全。2026-09-03 已复制到 `assets/game/images/campus-network-login.png` 并按规格允许的方式等比例缩小到 1293×740；源文件 SHA-256 为 `79afff8e70302bf4273e06484bd176dcdf38878fb60c83742be6174732541e6e`，运行时副本为 `348b2955038dd97b7cb1e310d07760ce96f9461e289eb0eef34ae1af4b2bfb72`。运行时截图再次确认输入框为空、页面布局和文字锚点仍可辨认。正式发布包仍须再做一次目视检查。

## 实现约束

- 不让生成模型重画，不用 MATLAB 文字和矩形重新拼一张近似页面；
- 复制为 `assets/game/images/campus-network-login.png` 后，只允许等比例缩放、必要裁边和压缩；
- MATLAB 使用图像对象显示、移动和旋转该 PNG；
- 碰撞平台使用与画面对应但彼此独立的矩形数据，不根据截图像素自动推断；
- 页面不获取键盘焦点，不开放输入框，不调用浏览器，不连接 `10.0.0.55`；
- 用卡片边框、投影和落下动画表明它是游戏里的实体截图，不是真实认证弹窗。

# 当前项目状态与接续入口

更新日期：2026-09-04

## 当前结论

第三轮试玩返工已在分支 `codex/gameplay-round-2` 完成并推送。运行时不再把流程拆成玩家可见的四关，而是在同一世界状态中依次经过 MATLAB 坐标轴、北湖、校园网页面、南北交通、乐学和 Lucy 河；破防值、冰红茶、绳、检查点和已完成事件会按规格跨区域保留。

截至第三轮视觉证据提交 `7e68f9d`，相对首版原子基线 `3d893fe` 含 63 个提交；相对远端 `main` 共 98 个提交。车流、北湖延长、羊驼捷径、校园网页面、乐学双页面和视觉证据均为独立提交。审计到的作者邮箱只有 `explorerzkb@gmail.com`。

开发机 MATLAB R2025b 的 Code Analyzer 为 0 个问题，统一冒烟测试通过；1280×720、乐学任务全开的连续世界最新强制绘制结果为 23.7 FPS，通过 20 FPS 低配门槛，但低于约 30 FPS 的常规目标。第三轮十张程序化机制截图已逐张打开检查，能够区分延长后的北湖动物、羊驼红桥捷径、完整校园网页面、可通行车流、两张原位乐学页面和 Lucy 终点。

首轮推送曾被 GitHub 私人邮箱保护以 `GH007` 拒绝。2026-09-04 再次按用户要求推送时已经成功：远端分支 `origin/codex/gameplay-round-2` 建立，首次成功推送点为 `6496ce8`；GitHub 提交接口确认其归属账号 `explorerzkb`，提交邮箱仍为项目固定的 `explorerzkb@gmail.com`。没有改用其他邮箱或重写历史。

这些是本地工程证据，不等于目标 Windows 电脑上的真实双人验收。当前画面仍是用来判断机制与空间关系的平涂灰盒，不满足最终“校园元素细致且一眼能辨认是学校景色”的美术标准。

## 当前冻结玩法

- 两名玩家共用键盘，控制同体型、同能力、同碰撞体的无腿无叶小梨；
- 两人由松弛时不施力、绷紧后产生拉力的普通绳连接；
- 一张连续横向地图从 MATLAB 坐标轴推进到 Lucy 河，不显示关卡卡片或逐段清空状态；
- 北湖延长为 52 世界单位，包含四只鹅、四只鸭和可选羊驼喷嚏红桥捷径；
- 校园网完整页面成为可攀爬、可按压的物理空间；
- 南北交通允许喷泉／北理桥和红绿灯两条路线；红绿灯含安全等待区、全红清空、行人绿灯和对岸安全区；
- 乐学区域按两张 2852×1654 原图的统一像素映射保留选课页与首页布局，包含六格倒计时、原位共同按钮、通知任务和“我的课程”存档卡；
- 当前破防值与累计失败分开，冰红茶可降低破防值并短时强化两名玩家；
- 只做单局检查点，不做磁盘存档、写实死亡、战斗、真实登录或联网服务。

详细行为以 [`game-spec-v1.md`](game-spec-v1.md) 当前 v2 内容为准。

## 已验证的第三轮本地状态

- 开发机：Apple Silicon Mac，MATLAB R2025b；
- `runCodeChecks`：当前 `.m` 文件 Code Analyzer 结果为 0 个问题；
- `runSmokeTests`：输入、碰撞、普通绳、连续区域、破防与冰红茶、延长北湖、羊驼完整飞行落桥、校园网机关、红绿灯端到端通行、页面像素映射、乐学按钮实际跳跃可达、安全复活和不可见窗口生命周期全部通过；
- `runPerformanceCheck`：任务风暴激活的连续世界最新强制全帧为 23.7 FPS @ 1280×720，通过自动化 20 FPS 下限；
- 隐私关键词扫描未在仓库中发现两张用户截图里的姓名、学院或访问日期；
- MATLAB 源码未发现网络 API，核心游戏不会访问真实校园账号、登录页或选课服务；
- 第三轮十张机制截图及逐项检查见 `docs/visuals/runtime-snapshots-round3/` 和 `docs/visuals/continuous-world-round3-review.md`；第二轮复核仅保留为被真人试玩推翻前的历史证据。

## 用户提供的新参考

2026-09-03 实际打开两张 2852×1654 的乐学／选课页面截图。它们支持教程条目、课程卡、通知数字、倒计时和开始选课按钮的交互设计，但含姓名、学院、访问日期等个人信息。用户随后把原图复制到 Git 忽略的 `assets/reference/private/lexue/` 供本机核对；它们不进入运行时或提交。证据、像素框与使用边界见 `docs/visuals/lexue-interaction-source-review.md`。

## 下一步

1. 在目标 Windows 11 电脑进行真实双人功能验收：多键冲突、125%／150% 缩放、连续三局、复位、暂停和退出；
2. 让两位玩家从头到尾试玩，记录流程时长、路线理解、卡住位置和冰红茶使用时机，再据实调参；
3. 在机制稳定后进入最终美术阶段，按视觉空间 SOP 补足建筑、道路、桥端、岸线、校园导视和生活细节，不能把当前灰盒截图当成最终效果；
4. 机制与文档已保存到远端开发分支；目标 Windows 与真人试玩通过后，再决定是否建立 Pull Request、合并 `main` 和创建版本标签。

## 最近接续点

用户已经确认 `config/gameConfig.m` 中 `cfg.physics.gravity` 从 `-10` 改为 `-20` 是有意调参，目的是让一个人同时测试两个角色时更容易控制。该变化通过 Code Analyzer 和统一冒烟测试后，已作为独立提交 `9949558` 保存。自动测试只能证明现有规则与机关没有明显回归；实际操作是否更轻松仍以用户手感为准。

2026-09-04 用户实际试玩后确认三项返工：红绿灯车流目前无法正常通行；北湖动物段过短且羊驼没有形成可感知收益；校园网与乐学页面必须尊重原页面布局和控件位置，使用统一像素坐标映射，不能为了玩法擅自重排。此前 `continuous-world-round2-review.md` 中交通与页面“通过”的结论已被真实试玩推翻，只保留为第二轮历史证据。

第三轮代码级返工接续点为 `7e68f9d`：车流端到端通行、52 单位鹅鸭段、羊驼红桥捷径、校园网完整原图映射、乐学双页面脱敏像素映射以及十张复核截图均已提交并推送。下一步优先在目标 Windows 电脑从头到尾真人复测；若车流、按钮高度或节奏仍有问题，以真人结果继续返工，不能用本轮自动测试覆盖用户体验。

## 决策记录

- 2026-09-03：首轮代码按功能重建原子提交；后续不再把整个骨架或多个机关作为一个功能提交。
- 2026-09-03：本仓库新提交固定使用 `explorerzkb@gmail.com`；GitHub 私人邮箱保护导致的推送拒绝保留为真实阻挡。
- 2026-09-03：玩家可见的四关结构被连续校园世界取代；内部区域只承担组织、测试和性能职责。
- 2026-09-03：破防水改为热带风味冰红茶，当前破防值与累计失败统计分离。
- 2026-09-03：两张含个人信息的乐学截图只作结构参考，不进入 Git 或运行时资产。
- 2026-09-03：第二轮不进行最终视觉质感优化；此前“校园元素细致可辨认”的最终要求继续有效。
- 2026-09-04：第二轮连续世界机制、自动测试、性能压测和程序化机制截图在本地完成；目标 Windows、人类双人试玩和最终校园美术仍是明确未完成项。
- 2026-09-04：检查点复活除了恢复角色与绳，还会把车辆、红绿灯、喷泉和通知任务恢复到安全瞬态，避免刚复活即再次受击。
- 2026-09-04：`codex/gameplay-round-2` 已成功推送，GitHub 作者归属核验通过；当前未创建 Pull Request、未合并 `main`、未打发布标签。
- 2026-09-04：用户确认将重力从 `-10` 调至 `-20`，用于降低单人同时测试双角色的操作负担；自动检查通过后独立提交。
- 2026-09-04：第三轮返工以真实试玩为准：重做车流可通行状态机、延长鹅鸭区域并让羊驼通往明确捷径、按原截图像素位置重建校园网与乐学交互。
- 2026-09-04：第三轮六项改动与十张视觉证据已分别提交并推送；页面匿名化只替换原卡片内部内容，不允许重新排版。最新 Mac 性能通过低配门槛，但常规 30 FPS 仍待后续处理与目标机验证。

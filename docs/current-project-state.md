# 当前项目状态与接续入口

更新日期：2026-09-11

## 当前结论

当前交付入口：`startGame('InputMode','pose')`，说明见 `docs/pose-control-windows.md`。重装后原生模型与摄像头依赖已实际可用；下方“未安装／重装中／尚未接入”段落为保留的过程前态，不是当前结论。模型选择、后台与游戏接入已实现，用户不参与现实动作，因此真人准确率和体感通关仍未验证。

2026-09-11 完整物理负载性能补测：此前近 30 FPS 的夹具仅推进场景机制。加入真实碰撞、绳子、道具物理后，本轮五场景为 29.11／22.90／28.03／30.00／29.79 FPS（`full-physics-pose-render.txt`），校园网显著未达目标，正在定位。报告中的校园段 2032.9 ms 最大软件延迟含跨场景预热期间遗留样本，已找到计时边界问题并修正；该历史数值不作为真人响应证据，也不删除原记录。相机没有可校准的两人站位，该轮有效双人关键点率为 0，不能声称动作识别通过。

游戏内现已实现固定大小的数值计时聚合：退出／切键盘写入 `pose-validation-results/`，分开统计采集调用率、每个相机区域有效关节率、总推理率、每场景绘制／物理计数及新样本至物理／drawnow 的软件延迟；不保存相机图片、不冒称曝光到屏幕完整响应。合成计时及状态回归、Code Analyzer 通过，实际 UI 退出保存通过。

2026-09-11 身份恢复安全补正：检测到重叠／身份不确定后锁定为“按 C 重新校准”，不靠重新左右站开自动恢复身份；校准界面 C 会清除旧方向／试跳核对。合成回归、游戏事件回归及 Code Analyzer 通过（`telemetry-checks.txt`）；真实后台取消后安全释放并重新打开摄像头通过；完整冒烟、连续序章及四条逻辑路线通过（`final-regression.txt`），没有两名真人证据。

2026-09-11 Windows 等待节拍已定位并修复：100 次实际测量，JVM `parkNanos(250000)` 中位 15.599 ms、最大 16.232 ms；改为随仓库提供的 .NET 小助手，使用进程私有的高精度 waitable timer，不修改全局计时设置、不忙等。等待中位 0.619 ms、最大 1.741 ms。无识别负载的五场景可见绘制改善为 29.90／30.00／26.00／29.98／29.80 FPS；交通仍有 124.5 ms 长帧，**未通过全五场景稳定 30 FPS**。初轮 27.9–29.4 FPS 与原键盘失败记录保留。性能验证入口 `runPoseRenderCheck` 单独按体感 30 Hz／60 Hz 报告帧间隔和长帧，不篡改旧 40／50 FPS 测试。全源码 Code Analyzer 零问题；后台负载复测和交通瓶颈分析继续进行。

2026-09-11 游戏接入完成首轮验证：`startGame('InputMode','pose')` 使用随仓库附带的原生 SinglePose 模型，显示目标 30 Hz；键盘默认保持 50 Hz，物理始终 60 Hz。初始校准显示镜像站位、起跳余量、水平稳定和逐人左右／试跳核对；K 切模式、C 重校准，喝茶／暂停／复位／退出仍用键盘。事件只在物理步消费，空中不排队，暂停／序章／复活／结算通过 epoch 丢掉在途旧结果。`testPoseGameInput`、原键盘映射、合成体感测试、TestMode 生命周期和 Code Analyzer 通过。真实启动校准 UI 收到摄像头数据后自动 Q 等效退出，随后摄像头可重新打开；没有真人校准通过或整局手感声明。日志 `integration-checks-2.txt`、`live-startup.txt`。五场景体感性能仍在测。

2026-09-11 后台阶段通过：MATLAB Processes 单工作进程独占摄像头／模型，主进程非等待轮询，一次最多一张请求，无图像积压。真实 HD Camera 640×480 + 双区域 SinglePose：100 对结果，每人 13.47 Hz，采集调用至消费者中位 75.9 ms；轮询中位 0.260 ms、最大 13.282 ms。不是曝光到显示延迟，不能声称达成真人手感。关闭本任务自建并行池后摄像头重新打开成功，全源码 Code Analyzer 零问题。另单独 1280×720 连续采集 60 帧／3.104 s = 19.33 Hz；这是采集调用率，不是硬件声明 FPS。证据 `pipeline-checks.txt` 与 `camera-probe.txt`。

2026-09-11 模型兼容性／独立静态小样通过：官方 SinglePose Lightning 3.tflite 与 MultiPose float16/1 均在 MATLAB 原生接口执行。MultiPose 需将输入元数据固定为 160×256，并显式启用 Windows uint8 推理；工具只改变两个尺寸向量指针、追加 40 字节，不改权重／算子，原文件保留。公开单人照片拼成双区域的对照（不是两真人质量验收）通过鼻／踝位置断言：双 SinglePose 每对中位 41.3 ms、最大 50.5 ms，每人理论更新上限 23.83 Hz；MultiPose 中位 268.3 ms、最大 376.6 ms，每人上限 3.77 Hz。当前优先双区域 SinglePose，小样与 Code Analyzer 通过，真人质量及端到端响应仍未知。详见 `model-comparison.txt`。

2026-09-11 接续：用户要求每完成独立功能即提交，允许本地提交、不推送；不再要求用户配合现实动作。新安装复查已能枚举 HD Camera，TFLite 接口已安装，SinglePose 实际加载与零输入预测通过。控制核心重跑 `testPoseController` 与全源码 Code Analyzer 均通过（`docs/validation/pose-control/core-checks.txt`）。MultiPose 官方文件在本机加载为动态 1×1×3，直接传 160×256 被接口拒绝；下一步核对固定输入方案，尚不能称其推理通过。原始环境、失败记录保留为重装前证据。

2026-09-11 Windows 体感开发开始：从远端仍一致的 `207a0129c11c2e393230d0aeb524ce3bd292baf4` 建立 `E:\matlabHi-pose-control` / `codex/pose-control` 独立工作树。原仓库 `E:\matlabHi\repository` 干净、远端正确。未提交、未推送、未合并；本地 Git 作者邮箱设为 `explorerzkb@gmail.com`。不读 archive、不依赖旧 dist、不补做 Mac 检查图或美术。

真实环境：Windows 11 专业版 22621；i5-13500H（12 核／16 线程）、31.73 GiB、Intel Iris Xe；Windows PnP 报 HD Camera OK，但不等于 MATLAB 已能采集。MATLAB R2025b (25.2.0.2998904) 批处理可运行，Deep Learning / Parallel Computing / MATLAB Coder / Image Processing / Computer Vision 工具箱可见。`webcamlist` 未定义；`loadTFLiteModel` 实际调用报 `gpucoder:cnncodegen:missing_tflite_support_package`；`TFLITE_PATH` 为空。摄像头支持包、TFLite 接口与运行库尚未就绪，真人识别和模型性能尚无结果。

体感显示按用户最新要求为 30 FPS、物理 60 Hz；下方历史 Mac 40／50 FPS 数据及失败原样保留。原版 Windows 自动基线正在执行，输出在 `docs/validation/pose-control/keyboard-baseline/`；测试中汇总器的模拟三人次／缩放 PASS 是夹具，不是本机真人证据。

待试玩默认方案：主动放下双臂或明显离开胸前姿势立即释放该玩家方向并禁止新跳跃，持续离姿暂停整局；稳定重摆、清事件、倒计时恢复。所有识别阈值属工程初值，未通过真人验证。两名真人整局、误触／漏检、疲劳、窄平台与上车时机仍待测。

2026-09-11 本轮自动验证补记：原始键盘 Code Analyzer 零问题、全部冒烟和四条输入旅程通过（59.95／72.30／59.97／72.28 s）。五场景强制绘制 19.3／43.5／33.4／39.0／32.3 FPS，原 40 FPS 门槛失败，且北湖不足体感目标。另次定速 4.4／19.7／13.9／15.8／15.0 FPS 失败；该次存在其他 MATLAB 与安装活动，不作空闲机器结论。原日志保留。独立控制核心首轮合成关键点测试及 Code Analyzer 通过，覆盖方向、事件、假动作、恢复和坐标逆变换；后续编辑需重跑。

用户正在重新安装 MATLAB：已停止本任务 MPM 安装进程，不再运行或修改 MATLAB 安装。官方 MPM 曾进入安装阶段但未报告完成；重装后必须重新盘点支持包，不能沿用此前探测结论。当前继续纯代码与文档工作，待用户告知重装完成后再运行。原生模型小样未通过，暂不越过阶段门接入游戏。

2026-09-11 路标文案调整：主文案改为“大跳可上桥”并居中，删除右上箭头；保留位置、尺寸、颜色和“先喝冰红茶”。

2026-09-11 每道车流补正：统一四条车道边界与车辆中心，每道五辆，消除右端多画出的空车道；路外纵深循环缩到 ±20，初始五车间隔 8。六个灯周期逐物理帧检查每道都有可见车体，每道均经过过街断面；绿灯过街、检查点复位、交通信号、车辆伤害与 Code Analyzer 已通过。总览与左右默认镜头截图已目视通过。最终统一冒烟与四条输入旅程全部通过，路线用时 59.95／72.30／59.97／72.28 秒，包含剧情复位与第四条北湖失误恢复。五段强制绘制最低 49.3 FPS，定速最低 42.4 FPS，均过 40。证据见 `visuals/traffic-road-bounds.md`；Windows 真人未测试，旧 dist 未更新。

2026-09-11 车流越界已修复：用户截图中的悬空车来自未裁剪的纵深投影，原车身纵坐标达 -5.33 至 6.97，道路仅 0.65 至 4.7。现在道路与车流共用显示边界，逐个车身部件裁剪并删除退化面；车流数量、速度、信号和地面碰撞不变。Code Analyzer 零问题、全部冒烟通过；实际打开桥上和路口两张运行截图，车不再露到桥顶或路外，桥墩遮挡正常。六周期渲染边界、离场和过街保形断言通过。五段强制绘制为 40.6／50.1／40.8／44.3／37.0 FPS，校车段未过 40，不能宣称全地图性能通过，也未确认失败原因。另次定速完整绘制五段为 47.6／48.8／49.0／49.6／49.3 FPS，通过但不撤销前次强制检查失败。证据见 `visuals/traffic-road-bounds.md` 与同名目录；Windows 真人未测试，旧 dist 未更新。

2026-09-11 路标位置纠正：上一版牌面横跨 x112 跑道／公路交界。整牌右移 1.3，牌面范围改为 x112.5—116.3，立柱 x114.4，全部落在公路侧，距交界至少 0.5；牌面同时下移到 y3.1—4.45，避开公路上沿与绿化的交界。

2026-09-11 路标追加修订：用户要求更小并融入环境，牌面从 6×2.15 缩到 3.8×1.35，改灰绿底、米白字和细立柱；保留两行提示与右上箭头。

2026-09-11：按用户要求在北理桥左侧增加“大跳上桥”醒目路标，附右上箭头和“先喝冰红茶”。保持 v30 跳跃与碰撞参数。Code Analyzer 零问题，入口实际绘制图已目视通过：黄底深字、右上箭头、落地立柱均清楚，未挡住双梨或行人灯。记录见 `visuals/bridge-jump-sign.md`。

2026-09-11 北湖天空接缝修复：用户截图中 y=12.8 的横向硬边来自北湖不透明背景与全局云图直接相接。现将北湖上缘按实际世界坐标的云图做静态合成，左右边缘也使用实际底图，东西重叠处纳入西段已合成像素。只在初始化计算，不增加逐帧透明混合、不改碰撞或镜头。三个抬高镜头已目视检查；Code Analyzer 零问题，五段强制绘制最低 43.3 FPS。完整冒烟复测与证据见 `visuals/sky-seam-v31.md`，不代表 Windows 真人验收。

v30 最终验收补记：00:37:31 启动的完整开发 Mac 验收通过，Code Analyzer 零问题、全部冒烟、900 帧连续飞行、四条按键路线均通过。路线用时 59.95／72.30／59.97／72.28 秒，前三条仅强制校园网复位，第四条另含北湖失误恢复，无校车致死。960×540 强制五段 56.7／73.0／48.4／73.7／52.3 FPS，定速完整绘制 47.8／48.9／47.3／40.8／48.7 FPS；最低 40.8 余量较小，不宣称稳定 50 FPS。前一轮北湖定速 35.9 未通过，原日志保存在 `visuals/playability-v30/acceptance/first-full-performance-failure.txt`，不把后续通过当作已证明低帧原因。云图增加 bilinear 显示，已打开上升、高空、落地与长卷右端截图复核；细节见 `visuals/playability-v30.md`。最终资源、代码和证据为本轮交付，旧 dist 未更新，真实 Windows 双人验证仍未执行。

2026-09-11 v30 正在最后验收：校园镜头比例改为 0.50，去树长卷与自然薄云已接入，天空纹理覆盖高空并随世界镜头连续移动，右侧道路延续到新终点 x426 之后。校车从 x282 起循环、车身净间隔 20，复活保护三秒；检查点七重排车距，静止五秒不受伤。北理桥桥面升至 6.5，普通跳跃不可达、茶跳可达，攀爬撤销；跑道后首次到达为行人红灯，20 辆纵向车辆。自行车等待双梨落地并与车身实际接触才触发飞行，保留惊恐表情和地面撞击闪光。四条逻辑按键路线通过，本轮完整绘制与性能验收结果待下方补记。旧版本参数以本条和规格 v30 为准；旧 dist 包未更新，Windows 真人未测试。

2026-09-10 v29 十项实玩修订已完成开发 Mac 验收：北湖红桥遮挡、桥宽纵向交通与两端大灯珠、入口冰红茶、更远的文博飞行与惊恐表情、详细大巴与车尾上车容错、更远校园视角、空格长按一秒跳过开场检测。Code Analyzer 零问题、全部冒烟测试、21 组飞行落地、五种双人车尾起跳及四条完整按键路线通过。四条路线用时 48.02／53.63／59.93／67.43 秒，前两条仅剧情超时复位，后两条仍含失误后恢复，不宣称无伤。960×540 五段强制绘制最低 72.0 FPS，50 Hz 完整绘制最低 49.2 FPS。最新截图已目视复核，记录见 `visuals/playability-v29.md`。旧 dist 包未更新；本轮交付是仓库代码与资源。Windows 真人双人验收未执行。

2026-09-10 v28 已接入：保留 v27 长卷，建筑世界尺度放大 1.5 倍，入口从 x170 移到 x200，连续地面扩到 x323；长距离撞飞仍按实际起跳高度解算约 70° 弧线，安全区随之移动。升到 y12 后自行车隐藏且停止移动；到 y18 后镜头缓慢拉远到 0.78，角色屏幕尺寸小约 22%，碰撞盒不变。八辆校车、原速度 3，入口至通关线从 54 延至 81 单位；保留原有上车距离和双梨间隔，避免缩放检查点后过早起跳反复撞车。北理桥近侧桥沿、桥墩、栏杆遮挡角色，高翼板留在后方。正常无伤断网坠落现在复位前额外停留 1 秒；旧 5 秒参数仅在致命伤害分支生效，不能视为原正常断网显示时长。四条完整按键路线通过，分别 57.97 / 66.10 / 58.73 / 79.05 秒，均含失误后恢复，非无伤通关。最新强制绘制五段最低 64.7 FPS，飞行 85.2 FPS，默认 50 Hz 节奏五段均约 49.6 FPS。截图和验证见 `visuals/campus-pacing-v28.md` 与同名目录；Windows 真人验收仍未执行。

2026-09-10 v27 已接入并通过开发 Mac 回归：用户指出 v26 仍俯视且背景路与车辆道路重复，飞行突然切成天空。现改平视长立面与单条柏油路，建筑图和角色共用世界坐标；撤销独立飞行画面，镜头连续上移再落地。文博比例依据照片，不宣称测绘还原。Code Analyzer 零问题、完整冒烟测试、420 帧飞行镜头连续性检查通过；960×540 飞行强制绘制 91.2 FPS、校车 92.3 FPS、五段最低 66.5 FPS；按游戏 50 Hz 节奏完整绘制五段为 46.0 / 47.2 / 48.5 / 49.6 / 49.6 FPS。源图清晰度未降低；目标 Windows 尚未复测。证据见 `visuals/eye-level-v27.md` 和 `visuals/eye-level-v27/`；下方 v26 的固定屏幕背景轴、飞行藏地景及性能结果不代表 v27。

2026-09-10 v26 将末班校车段的固定满屏照片背景改为会随镜头连续展开的 3:1 长卷。长卷不使用超广角单点／两点透视，而把文博馆、道路和文化体育中心放在同一组正东—西、正南—北坐标轴的平行投影中；两栋楼没有相对旋转，同方向外墙、屋檐、路缘保持平行。中间保留窄水泥东西路与一至两车道的等宽柏油南北路，远景减楼、减树，飞机、展示广场和坦克集中在体育馆高挑檐下。用户在最终图上标出 x=66.2%、y=63.8% 与 x=38.9%、y=61.7% 两条不存在的斜路，现已删除并补回连续草地；y≈80.9% 的水平线只作为镜头沿卷移动的大致平行轴，不宣称其附近草地是真实空间。背景只承担非碰撞远景，前景校车道路、角色、路灯和通关逻辑未改。运行时按镜头在 181—241 的推进量从长卷左侧平滑取景到右侧，让视点随玩家沿卷移动，不再把同一张图固定贴满屏幕。Code Analyzer 零问题、完整统一冒烟测试和长卷移动断言通过。四次当前负载下的校车段强制绘制为 95.7／67.3／46.2／85.3 FPS，均过 40；但同轮全地图门槛因北湖最低 19.7—39.6 FPS（另一次交通 35.1）未通过，当时另一个项目的常驻 Node 进程持续占约一个 CPU 核，只能记录相关性，不能把本轮写成五段性能全过。验证与画面见 `visuals/last-bus-handscroll-v26/`；目标 Windows 真人复测仍未执行。

2026-09-10 v25 修复操场跑道与北理桥公路在 x=112 的双人接缝坠落。此前动态碰撞面每帧重建，而校园网／成功操场机制按两只梨的平均横坐标停更；领先梨先进入公路时，平均值会在落后梨离开跑道前越过 112，导致成功页跑道承托被整段撤掉。现在以两人中较小的横坐标判断退出，只有落后梨也越过 x=112、进入连续公路后才停止维护跑道承托。`testNetworkReachability` 新增“一梨 x=113.8 已上公路、另一梨 x=110.5 尚在跑道”的直接断言和 90 帧静止回归；修复前稳定复现承托消失，修复后 Code Analyzer 零问题、完整统一冒烟测试通过，五段强制逐帧最低 63.1 FPS。目标 Windows 真人复测仍未执行。

2026-09-10 v24 在继续审计缓存接线时发现一处画质回退：校园终段背景先由 `readGameImage` 按步长采样，创建图像时又重复执行相同步长。高画质档因此把 1672×941 源图误缩到约 418×236，而不是约 836×471；默认档的有效步长则从规定的 6 变成了 36。现已删除第二次采样，并在 `testCampusPresentation` 中直接断言显示纹理尺寸等于“一次采样”的理论尺寸，防止后续性能改动再次偷降画质。修复后的 1280×720 校车图已实际打开，建筑立面、草地、道路、车辆、角色、HUD 和路灯均清楚可辨。完整工作树回归通过，强制绘制最低 77.4 FPS，默认档与高画质档五段定速完整刷新均为 49.6 FPS；最终独立解压包强制绘制最低 74.5 FPS，两档五段也均为 49.6 FPS。目标 Windows 仍未执行。

2026-09-10 v23 继续优化启动链路，没有再降画质。启动剖析显示主要可控耗时来自同一批 PNG 在场景初始化时重复 `imread` 和重复降采样；现加入按“路径、采样步长、文件修改时间和字节数”失效的进程内采样图缓存。缓存保存实际使用的降采样图，不把所有原始大图常驻内存；文件发生变化会自动重读。新增测试覆盖采样尺寸、重复命中和文件覆盖后的失效。相同 MATLAB 进程内三次 `startGame('TestMode', true)` 为 4.353 / 0.793 / 0.700 秒；改前前两次为 4.427 / 1.026 秒，第二次启动约快 22.7%，冷启动未退化。最终工作树完整回归强制绘制最低 80.0 FPS，默认档和高画质档五段定速完整刷新均为 49.6 FPS；15 张 1280×720 高画质图重新生成，并实际打开北湖、校园网页、北理桥和校车四张核对，文字、角色、结构和车辆仍可辨认。最终独立包强制绘制最低 79.0 FPS，默认档定速五段均为 49.6 FPS；高画质档另一次包内复测最低 48.2 FPS，仍严格高于 40。目标 Windows 仍未执行。

2026-09-10 v22 补上目标机证据的防误报闭环。`runWindowsPlaytest` 会在每局前记录显示缩放、计划路线、玩家二键位、唯一局号和两名真人确认，游戏日志再写实际路线、五段 FPS 与是否通关，退出后两人分别确认画面可读性和操作响应。`summarizeWindowsValidation` 只有在 100%／125%／150% 三档自动检查与整局记录、至少三局、上桥／下路、方向键／JLI、每段严格高于 40 FPS、三份两人画面与操作确认全部齐全时才显示 PASS。夹具测试证明“FPS 日志齐但没有人工确认”仍为 INCOMPLETE，补齐全部证据后才 PASS；当前 Mac 目录实际汇总为 0/3、INCOMPLETE，不会误吞旧 Mac 日志。另试过把帧间 park 放宽到 1 ms，五段最低降到 47.2 FPS，CPU 短时采样也未改善，已撤销并保留 v21 的 0.25 ms。恢复后统一回归通过，强制绘制最低 74.2 FPS，五段定速均为 49.6 FPS。目标 Windows 仍未执行。

2026-09-10 v21 继续处理“帧率过关但程序仍白占 CPU”的问题。帧间空闲不再用 `Thread.yield` 忙等，改为 0.25 ms 的 JVM park；它不调用 MATLAB `pause`，不会额外触发完整绘图，同时仍以亚毫秒级轮询处理键盘回调。相同节拍测试、相同取样时点的 8 次进程 CPU 读数平均从约 207% 降到 155%，约少 25%。改后完整统一回归再次通过：强制绘制五段为 80.6 / 109.5 / 77.5 / 93.7 / 152.6 FPS，最低 77.5 FPS；默认档和 1280×720 高画质档五段 50 Hz 定速刷新均为 49.6 FPS。真正的 `startGame('ValidationMode', true)` 主循环在北湖连续 9.5 秒记录 469 帧，为 49.2 FPS。短按边沿仍由 `pendingKeyPresses` 保留到下一次 60 Hz 物理步，输入映射测试与四条完整输入旅程均通过。证据见 `visuals/performance-v21/`。目标 Windows 11 与真人阶段门仍未执行。

2026-09-10 v20 把绘制目标从 45 Hz 提高到 50 Hz，专门扩大“实际帧率高于 40”的余量；分辨率、纹理采样、角色细节和 60 Hz 物理／输入均未降低。完整统一回归再次通过，五段强制绘制为 71.6 / 92.1 / 69.1 / 82.9 / 132.3 FPS，最低 69.1 FPS；默认档 50 Hz 完整刷新五段最低 49.9 FPS，1280×720、背景步长 2 的高画质档最低 49.8 FPS。另用真正的 `startGame('ValidationMode', true)` 主循环在北湖连续 8.9 秒记录 445 帧，为 49.7 FPS。证据见 `visuals/performance-v20/`。目标 Windows 11 与真人阶段门仍未执行，不能把这些 Mac 结果写成目标机已通过。

2026-09-10 v19 发现并修正了“最大绘制能力过 40，但主循环不一定真显示 40”的证据漏洞。MathWorks 规定 `drawnow limitrate` 最多只更新 20 FPS，`pause` 又等价于一次完整 `drawnow`；旧主循环在计划帧后用 `limitrate`，又在每次轮询末尾 `pause(0.001)`，实际刷新依赖隐含副作用，密集北湖在一份节拍测试中曾降到 30.3–36.1 FPS。现在计划帧显式使用完整 `drawnow`，帧间只处理回调并让出执行权，不再重复强制刷新；JVM 首次调用在计时前预热，不混入 FPS 样本。最终工作树五段完整刷新节拍全为 45.0 FPS，强制渲染最低 71.1 FPS；最新独立解压包五段节拍也全为 45.0 FPS，强制渲染最低 69.2 FPS。另一次真实 `startGame('ValidationMode', true)` 主循环在北湖连续 10.0 秒记录 447 帧，为 44.8 FPS。证据见 `visuals/performance-v19/`。

2026-09-10 v18 补齐了目标机验收链：`validateWindows.bat` 会记录当前缩放比和屏幕信息，连续跑三轮五场景强制绘制，再引导两人实际按六组四键／五键组合，主键位与 J/L/I 备用键位分开判定。`startGame('ValidationMode', true)` 在窗口标题显示每秒实际绘制 FPS，并在退出时保存五段交互帧率；少于 2 秒的区段明确标成样本不足。开场五秒按键页也已能分别显示方向键与备用键位，不再把两者混成一个“玩家二按过”。当时 v18 工作树完整回归最低 77.5 FPS，独立包最低 76.7 FPS；当前包以上方 v19 与下方包信息为准。真实键盘组合、Windows FPS 和缩放仍必须到目标机执行。

高画质档也不再是“只能开，性能未知”：1280×720、背景每 2 像素采样在同一开发 Mac 连续三轮五场景最低 62.6 / 62.4 / 84.0 FPS。已用该档重新生成 15 张运行时图，并实际打开北湖动物、校园网登录、北理桥／红绿灯和校车路灯四张；文字、角色、背景建筑、水面、桥梁与车辆细节都仍可辨认。因目标 Windows 性能尚未知，且规格已冻结默认 960×540，本轮没有擅自切换默认档。

2026-09-10 v17 继续压缩初始化和交通绘制：两辆汽车与三名行人的物理矩形、碰撞和推挤状态保持逐个计算，显示端由 15 个对象合并为 6 个；相同分辨率的冰红茶纹理只解码和描边一次；170 以后才出现的校园背景轴、背景图和校车对象不再在开场隐藏创建。MATLAB 原先还会因主动访问一个不可见的坐标轴工具栏而建立整套 Web 按钮，现已删除该访问；按真实游戏的无菜单、无工具栏窗口测，冷首帧约 2.98 秒、共 270 个图形对象，世界层 220 个对象。曾实测把校园网、交通和自行车也改成镜头触发创建，虽然首帧世界层可降到 178 个对象，却在移动中产生约 0.88 / 0.34 / 0.21 秒的一次性停顿，故已完整撤销；当前仍在交出操作权前预建全部飞行前交互。统一验收对最终代码重新通过：Code Analyzer 零问题、完整烟雾测试、连续序章和四条真实输入路线全部通过；强制逐帧五段为 76.8 / 98.7 / 65.4 / 89.6 / 137.4 FPS，最低 65.4 FPS，随后两次完整复测最低分别为 62.1 和 82.1 FPS。开发 Mac 上的孤立 Node 服务仍持续占满约一个 CPU 核。15 张 v17 回归图已生成，并实际打开北湖、北理桥交通、自行车和校车画面；车辆、行人和原有纹理细节可辨认。默认画质仍为 960×540、普通背景步长 6、校园网页步长 2，物理和输入仍为 60 Hz，绘制目标 45 Hz，没有靠继续降画质换取结果。证据见 `visuals/performance-v17/`。目标 Windows 11 真人双人、实际 FPS、多键冲突、100%／125%／150% 缩放和连续三局仍未执行。

2026-09-10 v16 全程性能重构已通过开发机新门槛：默认绘制目标由 30 Hz 提高到 45 Hz，物理仍为 60 Hz；短按的左右移动、跳跃和道具输入会缓存到下一次物理步，不再因某次循环没有物理子步而丢失。渲染改为固定坐标轴加单一世界变换，HUD／背景留在屏幕层；0—176 的连续前景常驻，切区时不重建整段场景，只有飞行天空与 170 以后校园终段切换绘制模式。鸭鹅、学生、校车、自行车、云层和网格合并为更少的复合图元，镜头外背景纹理、平台装饰和无关机制停止送入当前帧；NPC 视觉约 22.5 Hz 更新，鸭鹅巡逻、碰撞、承载和受力仍按 60 Hz 物理步推进，只跳过与远处玩家不可能发生的接触检查。赛跑几何在首帧预热但离开视口时不参与合成。该版证据保存在 `visuals/performance-v16/`，已由上方 v17 取代。

2026-09-10 v15 背景遮挡修复：v14 将 ax.Children 批量重新挂入 hggroup 时反转了静态对象顺序，蓝天覆盖其余场景。删除该多余分组，保留原绘制顺序；开场与复活后的实际截图已恢复。补充画面中段逐颜色通道的空间方差检查，防止纯蓝背景被当作通过。保留 v14 前景南北路线与独立校园背景，未修改物理。性能问题仍未验收解决。详见 `visuals/campus-v15/`。

v14 验证：背景与横向可玩层切换通过，代码检查零问题；帧率 8.2–12.0 FPS，未达 20 FPS，不能沿用下方 v13 结果。包已更新并核验 125 文件，属于方向纠正版，尚非性能完成版。见 `visuals/campus-v14/review.md`。

2026-09-09 v14 用户纠正：v13 把背景道路误当成玩家路线，已撤销角色、公交和路灯的纵深投影。已认可建筑图仅作背景，人和班车在前景南北道路按上一版横向运动，恢复跟随镜头、正常体型与原 HUD。单向五辆班车、70° 撞飞、飞行藏地景和故障死亡 5 秒保留。下方 v13 的“向东”“共用投影”为被撤销的实现，不能作为当前规格。

本轮 v13 开发机统一验收已通过：代码检查零问题、完整烟雾测试、四条实际按键路线、14 次死亡绘制恢复、21 种飞行落地组合；960×540 公交段强制渲染 30.7 FPS，最后绘制修正补测 27.9 FPS。离线包已更新并校验 126 个文件（SHA-256 `1b7c87c7215a7a21f162810c57cfe55f6be8487b5424736c035745a1de59493e`）。未做 Windows 真人验收。完整日志见 `visuals/campus-v13/latest-run.txt`。

2026-09-09 v13：用户认可真实西立面参考生成的 `west-photo-based.png`，已接入去标注运行时版本。道路、班车、角色、路灯共用西侧朝东透视；五辆班车仅向东、间隔约 4.67 秒。自行车撞飞约 70°、明显加力，飞行只见天空和角色；校园网超时死亡停留 5 秒。旧 v11 背景已撤出绘制。验证与画面记录见 `visuals/campus-v13/`。下方 v11/v12 暂停与待接入描述为先前决策记录，由本条取代。

2026-09-09 联网朝向核验：已实际打开设计方供稿的体育馆西、北立面照片与带北箭头的一层平面图。西面有多片高低错落挑檐，北面亦有大挑檐和横窗；撤销此前把大挑檐玻璃面武断定为南侧、把西端画成单一斜顶墙面的判断。新增房山区政府完工报道文博外景，未标方位；可用光影、结构、背景联合推断，但未独立确证西立面。资料在 assets/reference/landmarks/orientation-research/README.md。暂停生成，先以真实立面校正形体。
2026-09-09 朝向复核：用户否定 west-to-east.png；生成器在换视点时改变了立面朝向，上一轮未拦截。最新 west-orientation-study.png 仅为固定建筑、移动相机的方向研究，西端门窗细节未获照片验证，不作为正式背景。已认可的空间参照仍为 concept-directions.png。

2026-09-09 最新视觉决定：用户认可 v12 少树、带南北标注的空间构图，正式视点改为两栋建筑西侧朝东。文博左/北，体育馆右/南，道路向深处延伸。用户提出可考虑等轴测，先生成该视点预览；不把预览当作已完成游戏投影改造。下方 v11 被用户明确否定，当前本地运行时仍接有该失败草稿，不能作为正式视觉交付，ZIP 尚未纳入 v11；后续替换须连同角色、车辆和碰撞显示投影一起处理。

2026-09-09 背景纠正 v11：用户指出 v10 只改标牌，没有修正背景。已撤掉文博、开敞广场、体育馆沿横向依次拼接的三张运行时背景，换成道路两侧关系的一张连续背景；删除大型方位牌和旧地面条纹覆盖。文博位于道路远侧，文体位于近侧、主体在相机后方，以前景局部表达；不再把文体立面放到文博右边冒充沿路相邻。道路、草坪与统一地平面共同绘制，碰撞与车辆伤害不变。该视角是玩法化表达，不是实景照片。记录见 visuals/east-west-road-v11-space-card.md。

2026-09-09 方位纠正：用户确认文博与文体之间的马路为东西向，约 3 米宽、两侧草坪。此前把建筑片区的相对位置推成道路南北走向，是错误推断，已撤销。运行时标牌改为水平道路示意，并同步修正规格、计划与参考索引；不从建筑出场顺序推断东西位置。

2026-09-09 用户选择降低画质解决帧率：默认改为 960×540、背景纹理采样步长 6，保留 30 Hz 绘制目标和原有物理步长。新默认在本机强制渲染实测 20.2 FPS，达到原有 20 FPS 检查门槛，但余量较小；这是降低分辨率后的结果。之前 1280×720 的 14–18 FPS 失败记录仍保留，不能用旧的 46.6 FPS 代替当前结果。目标 Windows 未测。

2026-09-09 死亡闪退已复现并修复：复活删除 race/bus 状态后，交互循环可在下一次 stepLevel 前立即 renderFrame，触发缺失 race 字段，窗口随异常清理关闭。现在复活函数返回前用当前 cfg、dt=0 完整恢复动态状态和碰撞。七个检查点各两次“死亡→倒计时→复活→立即绘制”共 14 例通过；车辆伤害未关闭。证据见 `visuals/death-recovery-v8/diagnosis.md` 和 `regression.txt`。本轮是闪退热修复，未把先前性能问题标为解决。

**本轮性能未过门槛：** 车辆与通关测试通过，但 1280×720 强制渲染在本轮机器上约 14–18 FPS，独立运行亦未达到 20 FPS。已合并道路纹理、减少镜头外重复隐藏，并保留原有建筑细节；不能引用前一轮 46.6 FPS 声称本轮达标。失败日志保留在 acceptance-v7，Windows 未测。当前包是功能修订版，不标记为最终性能验收通过。

2026-09-09 最新车辆与小路修订：文博至体育馆按约 3 米路宽、两侧草坪绘制。公交车车身扣 3 点，车顶安全；北理桥行驶汽车不再只在斑马线范围内判伤，检测实际车身与帧间扫过区域。道路外等候区移至 x116–117.4，位于车辆最左行驶边界 x118 之前；对应提示已改。自行车从 3 辆增为 9 辆，速度由约 8–10 提至 18–22，保留 0.9 秒预警和原有无伤飞行。

公交段检查点提前到 x186，复活位置 186.2/187.7；复活重置公交循环时钟，避免刚复活又被同一车流撞中。新需求撤销了旧测试中“车身接触也必须零伤害”的假设；恢复测试现检查 3 点伤害后复位。四条输入路线都在一次公交碰撞后恢复完成，前三条共复活 2 次（含剧情超时），第四条 3 次（额外北湖失误）。没有把这些路线说成无伤通关。证据见 visuals/acceptance-v7 与 visuals/road-vehicles-v7-review.md；截图见 visuals/runtime-snapshots-world-v7。

2026-09-09 操场追加：保留固定网页，右侧改为照片风格的砖红跑道、绿草坪、低树线和蓝天。八条跑道各一名学生，两梨到 x90 后统一起跑，x108 冲线；对手固定速度 7.71–7.78，普通梨跑速 8。真实输入测试：较慢一梨正常跑领先最快学生 0.542 单位，约半个身位；赛段 2.244 秒。按住 Space 用掉一瓶冰红茶后为 1.793 秒。停下会输，比赛不锁道路；赛前复位可重赛。

本轮以 `visuals/playground-race-v6/`、`visuals/acceptance-v6/latest-run.txt` 为证据。代码检查零问题、烟雾测试、四条完整路线均通过；第四条路线仍有一次北湖额外失误后恢复。强制渲染 46.6 FPS。生成提示词与视觉边界见 `visuals/playground-race-v6-generation.md`。Windows 真人验收仍未进行。

最新离线包覆盖同名 ZIP，包含 119 个逐一核验的文件；下方 v5 包摘要已过时。本轮最终包 SHA256 以 dist 中的 .sha256 文件为准。

本次用户截图修订已完成本机回归，证据以 `visuals/acceptance-v5/latest-run.txt` 与 `visuals/runtime-snapshots-world-v5/` 为准：

- 北理桥空气墙来自攀爬未完成时添加的隐形门，现已删除；零次、单人一次、单人四次攀爬状态下，桥下实际步行均通过。地面红灯仍使用可见栏杆，桥面通行不受影响。
- 保留重力 -25。羊驼后踢为 [24,30]，孔雀为 [4.5,28]；真实冲量及绳子传递的水平速度保留 1 秒，不再被普通跑速上限立即抹掉。检查点复位会清除此状态。普通跳跃速度由 12.2 调到 15，使加大重力后的水沟跳跃仍可操作。
- 红桥碰撞左端由 40.3 移到 44.5，桥顶统一到 6.25；移除背景桥台，统一桥面、栏杆、木色和延伸。北理桥按实景双翼结构重做。文博、体育馆及中间开敞地带共使用三张无树墙新图。素材来源与提示词见 `visuals/user-feedback-v5-generation.md`。
- MATLAB R2025b Mac：Code Analyzer 零问题，全部烟雾测试通过，12 组动物双人带绳落桥通过，四条从出生点出发的完整输入路线通过。前三条仅有剧情超时一次；延迟 90 帧的第四条还有一次北湖失误，复活后完成，不隐藏这次失败。四条最终用时约 54 秒（游戏时钟）。1280×720 强制渲染实测 45.7 FPS。
- 曾怀疑地面穿透；保存前后帧核对后，实际是先落水再被绳子拖到地面下方。未以猜测修改碰撞器。普通跳跃调整后，延迟 30 帧的上下两条路线只剩剧情重试。

以下段落保留前一轮修复背景；其中 v4 包、截图和性能不代表本次 -25 版本。目标 Windows 双人真人验收仍未进行，本次结果不等于任意输入绝不卡死。

2026-09-09 用户实玩反馈重新打开空间修正：当前配置已由用户改为重力 -25，必须保留。修复攀爬空气墙、羊驼／孔雀弹射和红桥衔接，重做北理桥与文博—体育馆开敞过渡。此前 -15 的测试与包不能作为本轮验收。原有未提交修改全部保留；没有提交、推送或修改 Git 历史。

本轮修复了剧情飞行反向失控、高跳跳过必经剧情、检查点落在台阶内部、超时后复位碰撞延迟恢复、移动校车把梨压入道路、双车重叠重复携带，以及两只梨尚未进入体育馆就提前通关等问题。路口车辆改为沿地面水平行驶，避免把世界高度误作车辆远近；安全等待区不能因车辆车头进入危险区而被连带判死。

追加操作回归：已复现并修复第四次攀爬后在梯口正常跳跃被拉回桥面的错误；完成攀爬后不再重复吸附，掉回梯子下方可重新按四次爬上来。`testBridgeReentry` 修复前失败、修复后通过，四条完整输入旅程再次通过。该检查已加入烟雾测试。

追加连续性检查：行人原有取模循环及红灯重排会瞬移，现改为每次行人绿灯走到对侧、红灯保留位置、下一次绿灯返回。车辆端点回绕与切灯强制停车也已复现并修复，改为连续折返，剩余时间不足以驶离斑马线时提前在停止线等待。行人三个周期、车辆六个周期的逐帧位置和占道检查修复前失败、修复后通过；恢复回归、四条完整输入旅程与 Code Analyzer 再次通过。车辆折返是二维玩法压缩，不代表现实路口的掉头组织。

路口截图还发现“行人绿灯”文字配机动车红灯的表达冲突，现统一为行人信号：允许玩家过街时显示绿灯，其余阶段显示行人红灯，与实际通行判定共用状态。

独立解压验收补抓到行人推挤把玩家正常速度压到 2.6 的问题，导致给定步行输入不能在原有过街时间内通过。已改为只限制推挤、不抹掉玩家原有速度，`testTrafficTraversal` 和 `testTrafficCrowd` 通过。提供 `tools/build_validation_bundle.py` 生成只含运行代码、游戏资源与测试的离线包；SHA256 清单逐文件验证，不携带设计参考或私人参考文件。

北湖前景缺口、连续道路、网页入口台阶、桥面和支撑、树木、鸭鹅轮廓、绿白校车和终点标记均已更新。连续序章已接入：同一世界中的两条实际最小二乘拟合曲线变成原有梨图形，落地后交给玩家，不重建场景或角色。

**仍未达到最终交付：** 目标 Windows 11 的双人真人试玩、多键冲突、100%／125%／150% 缩放和连续三局稳定性尚未验证。当前输入旅程证明给定操作可以走通和失误后能够恢复，不能证明任意操作绝不会卡住，也不能代替真人对手感和画面质量的判断。

当前离线包为 `dist/matlabHi-windows-validation.zip`，包含 136 个项目文件和 1 份 `SHA256.json` 校验清单，共 137 项、27,277,000 字节；包 SHA-256 为 `31d7448fbf402533a25e4761f7303df4142479ba9eea6e474653d80c18b8ee8e`。ZIP 完整性、136 个文件的逐一哈希及清单与当前工作树的一致性均通过；在全新临时目录中对包内代码重新运行统一验收，默认档强制渲染五段为 80.3 / 100.8 / 74.5 / 85.0 / 154.6 FPS，最低 74.5 FPS，默认档与高画质档的 50 Hz 完整屏幕刷新五段均为 49.6 FPS。包内汇总器对空的 Windows 结果目录正确报告 0/3、INCOMPLETE。这些运行仍在开发 Mac，不是 Windows 实测。

空间依据见 `visuals/continuous-world-v4-space-card.md`，本轮验收见 `visuals/continuous-world-v4-review.md`。旧 v3 截图只代表修改前状态。

## 已进入运行时

### 全局状态、HUD 与角色

- 旧破防状态改成三颗共享爱心；普通冲撞扣一心，致命车辆和深渊清空，归零后延迟复位并回满；
- 冰红茶与血量完全隔离，只提供八秒移动强化；共享背包两格，北湖入口三瓶；
- HUD 使用三颗 8×7 像素心、两格真实冰红茶小图和六格绳力条；
- 绿色梨采用香梨式黄绿色，另一只采用暖奶油白鸭梨色；轮廓为高密度圆润曲线，并有固定的极轻明暗、微小浅坑、果柄和代码驱动表情；
- 全局图形抗锯齿关闭以满足低功耗帧率；梨体仍使用高密度曲线，因此几何轮廓保持圆滑，HUD 和边缘更接近像素游戏。

### 北湖与镜头

- 四只鹅和四只鸭都是动态实体碰撞体，可阻挡、承载并携带玩家；持续推挤约 0.45 秒后退让，只有满足主动迎面冲撞条件才扣一心；
- 羊驼身体可穿过，玩家进入其右后方时先抬腿预告再向右上后踢；旧喷嚏已移除；
- 孔雀周期性开屏并向上弹射，可把落到底层的玩家送回高桥；
- 横向死区为画面 28%—67%，纵向死区为 25%—72%；自行车飞行时镜头真实上移，落地后再回落。

### 校园网页面

- 网页是固定在世界坐标中的完整平面，不是弹窗；页面框架不淡化、不主动消失、不后退，也不锁镜头；
- 每局第一次有效接触登录区时，整张平面立即切成用户提供的 Chrome 深色超时页；承托同时撤除，双梨坠落后无伤复位；
- 第二次由两只梨共同站上登录区，原地加载约 0.8 秒，再把同一世界矩形切成用户提供的操场成功页；
- 成功后页面底部变成连续地面，角色世界坐标不重建，可直接沿操场向右；网页只因正常跟随镜头留在身后；
- 第一项学号为用户明确提供的 `1120250036`，另外两项保持待填；参考成功页中的 IP、流量、时长和余额已从运行时副本清除。

### 北理桥、撞飞与终点

- 操场连接到短道路；北理桥上路需喝茶后大跳登桥，下路需等待行人绿灯，进入任一路后锁定选择；
- 两路在北食堂、徐特立图书馆附近汇合；桥名全局统一为“北理桥”；
- 自行车／电动车剧情在进入路口后不可避免地触发，把两只梨撞向右上方，不扣爱心；
- 飞行落点为文博中心；五辆原创绿白校车仅向东行驶并作为移动平台，玩家需跳过三根低位双臂路灯；
- 2026-09-09 用户纠正：文博和文体之间是东西向马路，宽约 3 米，两侧草坪。撤销此前南北连通牌及其地图推断；不凭横版出场顺序推断哪座建筑在东或在西。
- 只有两只梨同乘一辆校车进入终点区才通关；体育馆之后没有新增尾段。

## 资产与视觉证据

- `assets/game/images/campus-network-timeout.png`
  - 用户提供的完整 Chrome 超时页，2112×1182；
  - SHA-256：`a8afeae42f7a43dc9a2850786538837654f62697f592aa63a71005b5a2a98d1a`。
- `assets/game/images/campus-network-authenticated.png`
  - 由用户提供的成功页制作的脱敏运行时副本，2742×1478；
  - SHA-256：`dc673e8a6168cc2eb5f632f7b8ce420d2d78737035ead4c6128e81e835ba7745`。
- `assets/reference/liangxiang/core-areas/north-li-bridge/user-north-li-bridge-reference.png`
  - 支持道路跨越、白色曲面、深色翼状结构、红色横带和桥墩等外观锚点，不支持精确通行路线。
- `assets/reference/transport/`
  - 两张实际打开的北理绿白大巴照片支持长途大巴比例、深色连续车窗、绿白分区和双轴轮位；照片没有进入运行时。
- `assets/reference/landmarks/`
  - 已实际打开文博中心、体育馆和国防教育展示照片；只用于外观锚点。
- `assets/reference/liangxiang/maps/liangxiang-campus-map-current.png`
2026-09-09 用户纠正：文博和文体之间是东西向马路，宽约 3 米，两侧草坪。撤销此前南北连通牌及其地图推断；不凭横版出场顺序推断哪座建筑在东或在西。
- `docs/visuals/runtime-snapshots-world-v3/`
  - 15 张连续世界运行截图，包括网页四态、双路线、自行车飞行、文博落点、东西向道路标牌、校车路灯和体育馆终点。
- `docs/visuals/runtime-snapshots-health-v3/`
  - 两张当前像素 HUD 截图；已更新为当前圆润梨、七检查点和六格绳力条。

本轮空间卡见 [`visuals/continuous-world-v4-space-card.md`](visuals/continuous-world-v4-space-card.md)，验收记录见 [`visuals/continuous-world-v4-review.md`](visuals/continuous-world-v4-review.md)。

## 本轮验证与复现

开发机为 Apple Silicon Mac、MATLAB R2025b。

```matlab
addpath('tests')
runAcceptanceChecks
```

统一入口运行 Code Analyzer、烟雾测试、连续序章、四条完整输入旅程和强制渲染性能检查。逐次输出保存在 `docs/visuals/acceptance-v4/latest-run.txt`，以该日志为数值依据。

- 剧情飞行覆盖七组双人起跳高度 × 无输入／左／右，共 21 组；
- 重生覆盖七个检查点；另测超时页面复位、重复上车与绳索纠正后的地面碰撞；
- 上桥、下路两条基准旅程只发生一次规定的网页超时重生，没有额外伤害；
- 带序章并改变出发时机的两条旅程均通关；各有一次北湖掉坑重生和一次规定的超时重生，属于失误恢复证据；
- 四条旅程均不使用冰红茶，完成两次认证尝试、一次剧情飞行、检查点 7 和双人同车终点；
- 实际打开 MATLAB 窗口检查过启动、暂停和 Q 退出，退出后回到命令窗口，无残留游戏窗口。不是完整真人双人试玩。

`runtime-snapshots-world-v4/` 是摆位检查图。`input-journey-upper-v4/`、`input-journey-lower-v4/` 是从出生点通过输入推进的事件截图，并保存输入轨迹和结束状态；两类证据不得混称。

## 接续顺序

1. 先读本轮验收记录与日志，继续关闭其中未完成项；
2. 在目标 Windows 11 完成两名真人整局试玩，覆盖两条路线、主／备用键位、三种显示缩放和连续三局；
3. 根据实玩调整碰撞、数值和提示，然后复跑统一验收入口；
4. 只有目标机与真人阶段门通过后，才判断是否满足最终交付。

另外两位同学的学号仍保持占位，不从截图或其他资料推断。

## 决策记录

- 2026-09-04：第一次登录固定显示完整 Chrome 超时页，撤除承托并无伤复位；第二次双梨共同登录才成功。
- 2026-09-04：校园网页面始终是同一世界平面；禁止淡化、主动隐藏、锁镜头或改变世界坐标。
- 2026-09-04：旧破防改为三颗共享爱心；冰红茶只影响移动；HUD 使用像素心、真实瓶图和六格绳力条。
- 2026-09-04：鸭鹅改为实体动态身体；羊驼使用背后后踢，孔雀承担回到高桥的弹射功能。
- 2026-09-04：桥名统一为“北理桥”，并保留北理桥／红绿灯双路线。
- 2026-09-04：北食堂路口加入不可避免的自行车右上撞飞；文博至体育馆用校车移动平台和路灯组成终段。
- 2026-09-09 用户纠正：文博和文体之间是东西向马路，宽约 3 米，两侧草坪。撤销此前南北连通牌及其地图推断；不凭横版出场顺序推断哪座建筑在东或在西。
- 2026-09-04：体育馆是终点，撤销良乡校门和“中关村校区”尾段。
- 2026-09-04：关闭 MATLAB 全局图形抗锯齿，将强制绘制提升到 20 FPS 门槛以上；保留高密度梨体曲线。
- 2026-09-04：本轮需求已实现，但连续 MATLAB 变梨序章和目标 Windows 真人验收仍未完成。

- 2026-09-09：连续序章、实体路面与桥梁支撑、21 组剧情飞行和完整输入旅程加入当前运行链路；最终交付仍受目标 Windows 真人验收约束。

# 连续校园世界实施计划

更新日期：2026-09-11
v30 验收补记：最终开发 Mac 完整验收通过，四条输入路线通过；强制绘制五段最低 48.4 FPS，定速最低 40.8 FPS。保留前次北湖 35.9 FPS 的失败日志，性能余量仍需目标 Windows 实测。本轮仓库交付，不更新旧 dist。
v30 当前更新：茶跳上桥取代攀爬、首次到达红灯、20 车纵向密集流、地面实际撞飞、三秒复活保护、20 单位校车净间隔、近镜头、去树长卷、全高度自然云层及终点延伸已接入；完整开发机验收进行中，证据放在 `visuals/playability-v30/`。本条覆盖下方旧版数值。
v29 当前更新：十项实玩修订已经完成，开发 Mac 完整验收通过。北湖近栏杆独立前层、桥下 30 单位过街与四车纵深运动、两端大灯珠、桥左冰红茶；文博入口移至 x300，高空 2 秒响应连续拉远到 0.32，飞行惊恐；大巴按现有两张照片细化为复合图元，车尾起跳增加限时宽容和一次连续补跳，开场空格一秒跳过检测。四条按键路线通过；960×540 强制绘制最低 72.0 FPS、50 Hz 完整绘制最低 49.2 FPS。当前证据集中在 `visuals/playability-v29/`，下文历史门槛数字不代表本轮。Windows 真人阶段门仍待执行。
当前阶段：空间与可玩性重构、连续序章、操场八道短跑、v24 全程性能／启动／画质优化和 v25 操场—公路接缝修复已接入；目标 Windows 真人阶段门未通过。v25 基于既有五段性能场景复跑，强制逐帧最低 63.1 FPS；v24 的完整性能证据仍见 `visuals/performance-v24/`。

v28 当前更新：更高更远的撞飞、30 单位入口缓冲、高空平滑缩小视角、1.5 倍建筑区／车程、8 辆校车、桥体与近侧栏杆遮挡、正常断网复位前多停 1 秒均已实现。下方旧 5 辆车与旧截图证据不是本轮参数；当前记录见 `visuals/campus-pacing-v28/`。

本文把 `game-spec-v1.md` 转换成可核对的实施状态。旧 v2 中的随机按钮掉线、镜头锁页、喷泉、乐学、Lucy 河、破防值和旧终点不得回流。

## 1. 已完成：状态与配置迁移

- [x] 保留用户工作树中的 `cfg.physics.gravity = -25`；
- [x] 用 `state.status.currentHearts` 和 `cfg.health.maxHearts` 取代 `breakValue`，所有伤害统一经过 `applyDamageEvent`；
- [x] 冰红茶只修改移动参数，容量两瓶、持续八秒，北湖入口三瓶；
- [x] 校园网使用 `login → timeout → retry → loading → success` 的确定性状态机；
- [x] 第一项学号配置为 `1120250036`，另外两项保持待填；
- [x] 当前世界以体育馆结束，主运行链路不再依赖乐学和 Lucy 河。

## 2. 已完成：物理与镜头

- [x] 鸭鹅进入动态实体碰撞、站立承载、移动携带和持续推挤退让逻辑；
- [x] 羊驼改为可穿透身体加背后后踢，孔雀加入周期弹射；
- [x] 横向 28%—67%、纵向 25%—72% 死区镜头在固定时间步中更新；
- [x] 北理桥／红绿灯路线锁定、自行车飞行、校车携带与路灯碰撞均使用小型状态结构；
- [x] 校车同车判定成为体育馆唯一通关条件。

## 3. 已完成：校园网页面

- [x] 登录、Chrome 超时和成功页实际进入同一个 `pageRect`；
- [x] 第一次触碰固定整页超时，不使用随机概率、不淡化；
- [x] 超时时撤掉网页承托，双梨坠落后无伤复位，并保留 `failureSeen`；
- [x] 第二次两梨共同站登录区，原地加载约 0.8 秒后成功；
- [x] 成功后加入连续操场地面，角色、页面和镜头世界坐标不重建；
- [x] 页面不锁镜头、不主动隐藏，玩家向右时自然把页面留在身后；
- [x] 成功页运行时副本只保留授权学号，其他账户状态使用离线占位。

## 4. 已完成：画面、HUD 与连续路线

- [x] 三颗像素爱心、两格真实冰红茶图标、六格绳力条和地区／复活信息；
- [x] 香梨黄绿色与鸭梨暖白色双梨，圆润曲线、轻微明暗、微小浅坑和代码表情；
- [x] 北湖连续画面、实体鸭鹅、羊驼高桥、孔雀下层回收；
- [x] 操场、北理桥四次攀爬与红绿灯二选一；
- [x] 北食堂／徐特立图书馆汇合和不可避免的自行车右上撞飞；
- [x] 文博中心安全落点、五辆单向绿白校车、三根双臂路灯和体育馆终点；
- [x] v27 平视长卷：完整文博长立面、自然天空、单条柏油前景道路，与公交车轮落点共用世界坐标；中间保留窄水泥支路，展品位于高挑檐下。照片比例可核验，测绘尺寸仍未知；
- 2026-09-09 用户纠正：文博和文体之间是东西向马路，宽约 3 米，两侧草坪。撤销此前南北连通牌及其地图推断；不凭横版出场顺序推断哪座建筑在东或在西。
- [x] 删除旧乐学、Lucy 河、喷泉和破防机制的当前运行代码、测试与文案入口。

## 5. 已完成：开发机验证

- [x] 先写直接断言，再改实现；
- [x] MATLAB R2025b Code Analyzer 0 个问题；
- [x] 统一烟雾测试全部通过；
- [x] 强制逐帧按 40 FPS 门槛分别检查北湖、校园网／短跑、交通、自行车和校车五段；v17 多轮最低 62.1 FPS，v18 工作树／当时独立包最低 77.5 / 76.7 FPS，v19 工作树／当前独立包最低 71.1 / 69.2 FPS；
- [x] 1280×720、背景 2 像素采样的高画质档在开发 Mac 连续三轮最低 62.6 / 62.4 / 84.0 FPS；重新生成 15 张高画质图，并实际打开北湖、登录页、北理桥／红绿灯、校车四张核对；
- [x] 新增定速完整 `drawnow` 的五场景检查，不再用最大吞吐量代替实际显示节拍；修复 `drawnow limitrate` 的 20 FPS 上限和每轮 `pause` 触发额外完整刷新的冲突。v20 将目标提高到 50 Hz，工作树五段节拍最低 49.9 FPS，高画质档最低 49.8 FPS，真实主循环北湖 8.9 秒为 49.7 FPS；
- [x] 将帧间 `Thread.yield` 忙等替换为 0.25 ms JVM park；配对的 8 次进程 CPU 采样均值从约 207% 降到 155%，完整回归定速最低 49.6 FPS，真实主循环北湖 9.5 秒为 49.2 FPS；
- [x] 静态图片按路径、采样步长和文件状态复用已采样结果；同进程第二次测试启动由 1.026 秒降到 0.793 秒，新增命中与失效测试；v23 独立包默认档定速最低 49.6 FPS、高画质档复测最低 48.2 FPS；
- [x] 删除校园终段缓存接线中的第二次采样，并断言显示纹理尺寸；恢复后工作树与独立包的默认／高画质五段定速刷新均为 49.6 FPS；
- [x] 操场跑道与公路接缝按落后梨的位置保留成功页承托；新增一前一后跨 x=112 和静止 90 帧不坠落回归，避免平均位置提前停更碰撞面；修复后五段强制逐帧最低 63.1 FPS；
- [x] 15 张连续世界、4 张页面四态、2 张 HUD 和 3 张梨表情截图已生成并实际打开；
- [x] 视觉复核记录真实参考能证明和不能证明的空间关系；
- [x] 隐私扫描不允许参考成功页的 IP、流量、时长和余额进入运行时。
- [x] 固定屏幕坐标轴，仅用一个世界变换移动可玩层，避免每帧重建 MATLAB 坐标轴交互状态；
- [x] 连续前景按镜头范围停更不可见机制与背景纹理；v27 撤销飞行／落地模式重建，校车和长卷一并预建，全程使用同一世界变换。天空遮罩只在初始化时计算，飞行镜头扩展高度并平滑居中跟随；默认绘制 50 Hz、物理 60 Hz，短按输入跨帧保留。

## 6. 尚未完成的阶段门

### P0：目标 Windows 真人验收

已准备 `validateWindows.bat` / `runWindowsValidation.m`：自动记录平台、屏幕信息、当前缩放比、三轮五场景 FPS 和六组四键／五键冲突结果。`runWindowsPlaytest` 记录每局缩放、路线、键位、两人确认、五段交互 FPS 及画面／操作判断；`summarizeWindowsValidation` 逐项审计覆盖，缺任一项都只报告 INCOMPLETE。这些工具尚未在目标 Windows 上执行。

1. 两名真人从北湖连续玩到体育馆，至少完成三局；
2. 核对主键位与备用键位的多键冲突；
3. 分别检查 100%、125% 和 150% 显示缩放；
4. 记录鸭鹅是否硬锁、网页是否误导、攀爬是否可读、撞飞是否晕眩、校车与路灯是否公平；
5. 只修复实玩暴露出的数值、碰撞和提示问题，再复跑全部门槛。

### 已关闭：连续 MATLAB 序章与局部软锁

- [x] `runPrologue` 已进入主链路，曲线直接变成当前梨图形，落到相同世界后交接操作；
- [x] 原始拟合由小型最小二乘求解得到，显示的指标由残差计算；
- [x] 网页入口台阶与七个检查点复位一致；
- [x] 剧情飞行覆盖不同起跳高度和方向输入，不能跳过触发；
- [x] 校车沿前景南北道路单向循环发车、单一车辆携带、车顶承托和底层安全回收；
- [x] 两条路线的基准输入及改变出发时机后的输入均通关，误操作恢复另列证据；
- [x] 短按键事件在渲染与物理更新之间保留，不被无物理步的一帧吞掉。

## 7. 非目标与提交边界

- 不实现真实校园网请求、真实密码、浏览器嵌入、战斗、复杂背包、跨局存档或精确校园导航；
- 2026-09-09 用户纠正：文博和文体之间是东西向马路，宽约 3 米，两侧草坪。撤销此前南北连通牌及其地图推断；不凭横版出场顺序推断哪座建筑在东或在西。
- 不用淡入淡出掩盖世界连接，不主动隐藏成功网页，也不在体育馆终点后追加校园路线；
- 只暂存本轮明确文件，不使用 `git add .`；任何提交固定使用 `explorerzkb@gmail.com`，但未经用户明确要求不自动提交或推送。

2026-09-09 v14 纠正：文博与体育馆的东西向小路只在背景中。可玩层为前景南北道路，恢复上一版横向角色/车辆和跟随镜头；撤销 v13 对玩家的纵深投影。

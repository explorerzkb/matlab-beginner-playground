# 版本管理规范

本文件规定本项目的 Git 与版本发布方式。目标是让每一次修改都能被理解、追踪、复现和必要时安全回退。

## 1. 基本原则

1. **小步提交**：一次提交只解决一个清晰的问题，避免把无关改动混在一起。
2. **先验证再提交**：运行与本次改动相关的检查或游戏流程，确认没有明显回归后再提交。
3. **提交可读**：提交信息说明“改了什么”，必要时补充“为什么改”。
4. **主分支保持可用**：`main` 只保留可以运行、可以展示或已经明确标记为稳定的代码。
5. **不提交敏感信息**：密码、访问令牌、个人数据、商业机密和本地配置不得进入仓库。
6. **历史可追溯**：不要为了“让历史好看”而重写已经共享到远程的提交历史。

## 2. 分支约定

- `main`：稳定主线，作为发布和演示的基础。
- `feature/<简短名称>`：新功能或新小游戏，例如 `feature/guess-number`。
- `fix/<简短名称>`：缺陷修复，例如 `fix/input-validation`。
- `docs/<简短名称>`：只涉及文档的修改，例如 `docs/update-versioning`。

开发时从最新的 `main` 创建短生命周期分支，完成并验证后合并回 `main`。分支合并后应删除，除非它承担长期维护职责。

## 3. 提交信息

采用简化的 Conventional Commits 格式：

```text
<类型>: <简短描述>
```

常用类型：

- `feat`：新增功能或小游戏
- `fix`：修复错误
- `docs`：修改文档
- `refactor`：不改变外部行为的代码整理
- `test`：新增或修改测试
- `chore`：工具、配置或维护性改动

示例：

```text
feat: add matrix quiz game
fix: handle empty user input
docs: explain local setup
```

描述使用祈使语气、尽量简短；如果改动原因不明显，可在提交正文中补充背景、验证方式和已知限制。

## 4. 版本号

发布版本采用 [语义化版本（Semantic Versioning）](https://semver.org/lang/zh-CN/)：`主版本.次版本.修订版本`，例如 `1.2.0`。

- **主版本**：出现不兼容的使用方式或重大重构。
- **次版本**：向后兼容地增加功能或新的小游戏。
- **修订版本**：向后兼容地修复错误、改进文档或进行小幅维护。

项目尚未形成第一个稳定发布前，可使用 `0.x.y`：此阶段允许接口和玩法发生较大变化，但仍应记录变化原因。

## 5. 标签与发布

每个正式版本在 `main` 上创建带注释的 Git 标签，格式为 `v主版本.次版本.修订版本`，例如：

```bash
git tag -a v0.1.0 -m "Release v0.1.0"
git push origin v0.1.0
```

GitHub Release 应说明：

1. 本版本新增或改进的内容；
2. 修复的问题；
3. 已知限制或不兼容变化；
4. 验证环境与运行方式。

## 6. 推荐工作流

```bash
git switch main
git pull --ff-only
git switch -c feature/<name>

# 编写代码并运行相关验证
git status
git diff
git add <明确的文件>
git diff --staged
git commit -m "feat: <description>"

# 推送分支并通过 Pull Request 合并
git push -u origin feature/<name>
```

合并前至少检查：功能是否按预期运行、异常输入是否有合理处理、文档是否同步、是否误提交临时文件或敏感信息。

## 7. 回滚与修复

- 尚未推送的错误提交：优先使用 `git commit --amend` 或交互式整理，但要确保不会丢失工作。
- 已推送且需要撤销的改动：优先使用 `git revert <commit>`，保留公开历史并生成可审计的反向提交。
- 只有在明确协调所有协作者后，才考虑对共享分支执行历史重写。
- 发布版本出现严重问题时，先在问题分支修复并发布新的修订版本，不直接删除已有标签。

## 8. 版本记录

每次发布在 `CHANGELOG.md` 中记录用户可感知的变化；版本记录按从新到旧排列，并将变化归类为 Added、Changed、Fixed 和 Known Issues。若项目规模很小，也可以先在 GitHub Release 中维护，但正式发布前应补齐 `CHANGELOG.md`。

